apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: leader-election-rolebinding
subjects:
- kind: ServiceAccount
  name: "vault-operator" # TODO: should include release.name
  namespace: "{{ .Release.Namespace }}"
